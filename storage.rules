rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Listing images: public read, writes only via Admin SDK (scraper)
    match /listings/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
